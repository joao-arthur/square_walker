import { scenarioParts } from '../scenarioParts';

function getModel() {
    return [
        [
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN
        ],
        [
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN
        ],
        [
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN
        ],
        [
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN,
            scenarioParts.TERRAIN
        ]
    ];
}

export const scenarioModel = {
    getModel
};
