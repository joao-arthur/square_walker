import { describe, expect, it } from 'vitest';
// import { noise } from './noise';

describe('noise', () => {
    it('should work', () => {
        expect(1 + 1).toBe(2);
    });
    //  console.log(noise());
});
