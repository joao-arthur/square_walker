import { chunckType } from '../../model/chunck';
import { modelType } from '../../model/model';
import { cameraType } from '../camera/camera';

export function output(camera: cameraType, model: modelType): chunckType {

}
